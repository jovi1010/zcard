# .env.local — ZCard / TAP
# Cards live in data/cards/[slug].json — no database needed.

# Optional: Cloudflare Worker endpoint (Manila edge sync)
CLOUDFLARE_WORKER_URL=https://your-worker.your-subdomain.workers.dev
CLOUDFLARE_WORKER_SECRET=your-shared-secret
